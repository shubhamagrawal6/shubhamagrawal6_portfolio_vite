name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          VITE_MY_RESUME_URL: ${{ secrets.VITE_MY_RESUME_URL }}
          VITE_HEALTHNOW_CERT_URL: ${{ secrets.VITE_HEALTHNOW_CERT_URL }}
          VITE_STORY_OF_MAKERS_CERT_URL: ${{ secrets.VITE_STORY_OF_MAKERS_CERT_URL }}
          VITE_OCI_CLOUD_CERT_URL: ${{ secrets.VITE_OCI_CLOUD_CERT_URL }}
          VITE_OCI_AI_CERT_URL: ${{ secrets.VITE_OCI_AI_CERT_URL }}
          VITE_MY_GITHUB_URL: ${{ secrets.VITE_MY_GITHUB_URL }}
          VITE_MY_LINKEDIN_URL: ${{ secrets.VITE_MY_LINKEDIN_URL }}
          VITE_MY_LEETCODE_URL: ${{ secrets.VITE_MY_LEETCODE_URL }}

      - name: Add CNAME for custom domain (if provided)
        env:
          CUSTOM_DOMAIN: ${{ secrets.CUSTOM_DOMAIN }}
        if: ${{ env.CUSTOM_DOMAIN != '' }}
        run: |
          echo "Adding CNAME from secrets.CUSTOM_DOMAIN"
          mkdir -p ./build
          echo "$CUSTOM_DOMAIN" > ./build/CNAME

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build
          publish_branch: gh-pages
          user_name: github-actions[bot]
          user_email: github-actions[bot]@users.noreply.github.com
